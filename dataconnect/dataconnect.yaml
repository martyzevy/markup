specVersion: "v1beta"
serviceId: "markup"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "markup-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
